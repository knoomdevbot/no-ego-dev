# Safe defaults for a profile distribution. Installers keep their own secrets in .env.
agent:
  max_turns: 120
  tool_use_enforcement: true
compression:
  enabled: true
  threshold: 0.60
memory:
  memory_enabled: true
  user_profile_enabled: true
display:
  tool_progress: true
